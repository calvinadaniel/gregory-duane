# Mega Panel Navigation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the broken CSS hover-only dropdown navigation with click-triggered full-width mega panels that close reliably.

**Architecture:** Three sequential changes — (1) swap CSS dropdown rules for mega panel styles in `style.css`, (2) append mega panel JS logic to `nav.js`, (3) use a Python script to replace nav markup identically across all 9 HTML pages in one pass. Mobile menu accordions are preserved unchanged.

**Tech Stack:** Plain HTML/CSS/JS, Python 3 for bulk HTML editing.

---

## File Map

| Action | File | What changes |
|--------|------|--------------|
| Modify | `style.css` | Remove lines 158–182 (desktop dropdown rules); add mega panel CSS |
| Modify | `nav.js` | Append mega panel open/close/outside-click/escape logic |
| Modify ×9 | All HTML pages | Replace `.nav-left` dropdowns with trigger buttons; insert 2 mega panel blocks; add 4 Women items to mobile menu |

**9 HTML pages:** `index.html`, `custom-suits.html`, `ready-to-wear-suits.html`, `about-us.html`, `handmade-shoes.html`, `tuxedos.html`, `wedding-dresses.html`, `gallery.html`, `appointment-form.html`

---

## Task 1: Update style.css — swap dropdown rules for mega panel CSS

**Files:**
- Modify: `style.css` (lines 158–182 replaced; new mega panel section appended)

- [ ] **Step 1: Verify the lines to remove**

```bash
cd "G:/coding/Claude POC Projects/gregory-duane"
sed -n '158,183p' style.css
```

Expected output — exactly these lines (confirming position before editing):
```
/* Dropdowns */
.has-dropdown { position: relative; }
.has-dropdown > a::after { content: ' \25be'; font-size: 8px; }
.dropdown-menu {
  display: none;
  position: absolute;
  top: calc(100% + 12px);
  left: 0;
  background: var(--surface);
  border: 1px solid var(--outline);
  min-width: 190px;
  padding: 8px 0;
}
.has-dropdown:hover .dropdown-menu { display: block; }
.dropdown-menu li a {
  display: block;
  padding: 11px 20px;
  font-size: 11px;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--white);
  opacity: 0.72;
  transition: opacity 0.2s, color 0.2s;
}
.dropdown-menu li a:hover { opacity: 1; color: var(--gold); }
```

- [ ] **Step 2: Replace the dropdown block with mega panel CSS**

In `style.css`, find and replace the entire dropdown section. Use the Edit tool: find the exact block starting with `/* Dropdowns */` through `dropdown-menu li a:hover` and replace with:

```css
/* Nav trigger buttons */
.nav-trigger {
  background: none;
  border: none;
  color: var(--white);
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 5px;
  opacity: 0.8;
  padding: 0;
  transition: opacity 0.2s, color 0.2s;
}
.nav-trigger:hover { opacity: 1; }
.nav-trigger.active { color: var(--gold); opacity: 1; }
.nav-chevron {
  font-size: 7px;
  display: inline-block;
  transition: transform 0.2s;
  color: var(--muted);
}
.nav-trigger.active .nav-chevron {
  transform: rotate(180deg);
  color: var(--gold);
}

/* Mega panel */
.mega-panel {
  display: none;
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: var(--surface);
  border-top: 2px solid var(--gold);
  border-bottom: 1px solid var(--outline);
  z-index: 99;
}
.mega-panel.open { display: block; }
.mega-inner {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 32px var(--page-margin) 28px;
}
.mega-grid-men {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 0;
}
.mega-grid-women {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0;
  max-width: 480px;
}
.mega-col {
  padding-right: 32px;
  border-right: 1px solid var(--outline);
  margin-right: 32px;
}
.mega-col:last-child {
  border-right: none;
  margin-right: 0;
  padding-right: 0;
}
.mega-col-label {
  font-family: var(--font-sans);
  font-size: 8px;
  font-weight: 600;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  color: var(--gold);
  margin-bottom: 14px;
}
.mega-link {
  display: block;
  font-family: var(--font-sans);
  font-size: 11px;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--white);
  opacity: 0.72;
  padding: 5px 0;
  transition: opacity 0.15s, color 0.15s;
}
.mega-link:hover { opacity: 1; color: var(--gold); }
.mega-sub {
  font-family: var(--font-serif);
  font-size: 11px;
  color: var(--muted);
  font-style: italic;
  margin-top: 4px;
  line-height: 1.5;
}
.mega-close {
  position: absolute;
  top: 14px;
  right: 24px;
  background: none;
  border: none;
  color: var(--muted);
  font-size: 20px;
  cursor: pointer;
  line-height: 1;
  padding: 0;
  transition: color 0.15s;
}
.mega-close:hover { color: var(--white); }
```

- [ ] **Step 3: Confirm mobile dropdown rules are untouched**

```bash
grep -n "mobile-menu.*has-dropdown\|has-dropdown.*mobile" style.css
```

Expected: lines ~242–260 still present, unchanged.

- [ ] **Step 4: Confirm no desktop `.has-dropdown:hover` rule remains**

```bash
grep -n "has-dropdown:hover" style.css
```

Expected: no output (rule removed).

- [ ] **Step 5: Commit**

```bash
git add style.css
git commit -m "feat: replace hover dropdown CSS with mega panel styles"
```

---

## Task 2: Update nav.js — add mega panel logic

**Files:**
- Modify: `nav.js` (append new logic; existing 38 lines unchanged)

- [ ] **Step 1: Confirm current file is 38 lines**

```bash
cd "G:/coding/Claude POC Projects/gregory-duane"
wc -l nav.js
```

Expected: `38 nav.js`

- [ ] **Step 2: Append mega panel logic**

Open `nav.js` and add the following block at the very end (after the existing resize listener):

```js

// Mega panel open/close
const triggers = document.querySelectorAll('.nav-trigger');
const megaPanels = document.querySelectorAll('.mega-panel');

function closeMegaPanels() {
  megaPanels.forEach(p => {
    p.classList.remove('open');
    p.setAttribute('aria-hidden', 'true');
  });
  triggers.forEach(t => {
    t.classList.remove('active');
    t.setAttribute('aria-expanded', 'false');
  });
}

triggers.forEach(trigger => {
  trigger.addEventListener('click', () => {
    const panelId = trigger.dataset.panel;
    const panel = document.getElementById(panelId);
    const isOpen = panel.classList.contains('open');
    closeMegaPanels();
    if (!isOpen) {
      panel.classList.add('open');
      panel.setAttribute('aria-hidden', 'false');
      trigger.classList.add('active');
      trigger.setAttribute('aria-expanded', 'true');
    }
  });
});

document.querySelectorAll('.mega-close').forEach(btn => {
  btn.addEventListener('click', closeMegaPanels);
});

document.addEventListener('click', e => {
  if (!e.target.closest('.site-nav')) closeMegaPanels();
});

document.addEventListener('keydown', e => {
  if (e.key === 'Escape') closeMegaPanels();
});
```

- [ ] **Step 3: Confirm line count increased**

```bash
wc -l nav.js
```

Expected: `~76 nav.js` (38 original + ~38 new lines).

- [ ] **Step 4: Commit**

```bash
git add nav.js
git commit -m "feat: add mega panel click-toggle logic to nav.js"
```

---

## Task 3: Update all 9 HTML pages — mega panel nav markup

This task uses a Python script to make three consistent changes across all 9 pages in one pass:
1. Replace `.nav-left` dropdown markup with trigger buttons
2. Insert two mega panel blocks after `.nav-inner` closing div
3. Add 4 new Women items to the mobile menu Women accordion

**Files:**
- Modify ×9: All HTML pages listed above

- [ ] **Step 1: Create the update script**

Create `scripts/update-nav.py` with this content:

```python
import os

pages = [
    'index.html', 'custom-suits.html', 'ready-to-wear-suits.html',
    'about-us.html', 'handmade-shoes.html', 'tuxedos.html',
    'wedding-dresses.html', 'gallery.html', 'appointment-form.html'
]

OLD_NAV_LEFT = '''      <div class="nav-left">
        <div class="has-dropdown">
          <a href="#">Men</a>
          <ul class="dropdown-menu">
            <li><a href="custom-suits.html">Bespoke Suits</a></li>
            <li><a href="ready-to-wear-suits.html">Ready to Wear</a></li>
            <li><a href="handmade-shoes.html">Handmade Shoes</a></li>
            <li><a href="tuxedos.html">Tuxedos</a></li>
          </ul>
        </div>
        <div class="has-dropdown">
          <a href="#">Women</a>
          <ul class="dropdown-menu">
            <li><a href="wedding-dresses.html">Wedding Dresses</a></li>
          </ul>
        </div>
      </div>'''

NEW_NAV_LEFT = '''      <div class="nav-left">
        <button class="nav-trigger" data-panel="mega-men" aria-expanded="false" aria-controls="mega-men">
          Men <span class="nav-chevron" aria-hidden="true">&#9662;</span>
        </button>
        <button class="nav-trigger" data-panel="mega-women" aria-expanded="false" aria-controls="mega-women">
          Women <span class="nav-chevron" aria-hidden="true">&#9662;</span>
        </button>
      </div>'''

MEGA_PANELS = '''
    <!-- Men Mega Panel -->
    <div class="mega-panel" id="mega-men" aria-hidden="true">
      <button class="mega-close" aria-label="Close men\'s menu">&#215;</button>
      <div class="mega-inner">
        <div class="mega-grid-men">
          <div class="mega-col">
            <div class="mega-col-label">Bespoke</div>
            <a class="mega-link" href="custom-suits.html">Bespoke Suits</a>
            <p class="mega-sub">Made to your exact measurements</p>
          </div>
          <div class="mega-col">
            <div class="mega-col-label">Ready to Wear</div>
            <a class="mega-link" href="ready-to-wear-suits.html">RTW Suits</a>
            <p class="mega-sub">Premium silhouettes, in stock</p>
          </div>
          <div class="mega-col">
            <div class="mega-col-label">Footwear</div>
            <a class="mega-link" href="handmade-shoes.html">Handmade Shoes</a>
            <p class="mega-sub">Handcrafted leather</p>
          </div>
          <div class="mega-col">
            <div class="mega-col-label">Formal</div>
            <a class="mega-link" href="tuxedos.html">Tuxedos</a>
            <p class="mega-sub">Black tie &amp; bridal</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Women Mega Panel -->
    <div class="mega-panel" id="mega-women" aria-hidden="true">
      <button class="mega-close" aria-label="Close women\'s menu">&#215;</button>
      <div class="mega-inner">
        <div class="mega-grid-women">
          <div class="mega-col">
            <div class="mega-col-label">Bridal</div>
            <a class="mega-link" href="wedding-dresses.html">Wedding Dresses</a>
            <a class="mega-link" href="#">Bridal Alterations</a>
            <a class="mega-link" href="#">Bridal Accessories</a>
            <a class="mega-link" href="#">Mother of the Bride</a>
          </div>
          <div class="mega-col">
            <div class="mega-col-label">Fashion</div>
            <a class="mega-link" href="#">Ready to Wear Suits</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile Menu -->'''

OLD_MOBILE_COMMENT = '    <!-- Mobile Menu -->'

OLD_MOBILE_WOMEN = '''      <div class="has-dropdown">
        <a href="#">Women</a>
        <ul class="dropdown-menu">
          <li><a href="wedding-dresses.html">Wedding Dresses</a></li>
        </ul>
      </div>'''

NEW_MOBILE_WOMEN = '''      <div class="has-dropdown">
        <a href="#">Women</a>
        <ul class="dropdown-menu">
          <li><a href="wedding-dresses.html">Wedding Dresses</a></li>
          <li><a href="#">Bridal Alterations</a></li>
          <li><a href="#">Bridal Accessories</a></li>
          <li><a href="#">Mother of the Bride</a></li>
          <li><a href="#">Ready to Wear Suits</a></li>
        </ul>
      </div>'''

for page in pages:
    with open(page, 'r', encoding='utf-8') as f:
        content = f.read()

    assert OLD_NAV_LEFT in content, f"nav-left pattern not found in {page}"
    assert OLD_MOBILE_COMMENT in content, f"mobile comment not found in {page}"
    assert OLD_MOBILE_WOMEN in content, f"mobile women pattern not found in {page}"

    content = content.replace(OLD_NAV_LEFT, NEW_NAV_LEFT)
    content = content.replace(OLD_MOBILE_COMMENT, MEGA_PANELS)
    content = content.replace(OLD_MOBILE_WOMEN, NEW_MOBILE_WOMEN)

    with open(page, 'w', encoding='utf-8') as f:
        f.write(content)

    print(f"✓ {page}")

print("Done.")
```

- [ ] **Step 2: Verify scripts directory exists, then run**

```bash
cd "G:/coding/Claude POC Projects/gregory-duane"
mkdir -p scripts
python scripts/update-nav.py
```

Expected output:
```
✓ index.html
✓ custom-suits.html
✓ ready-to-wear-suits.html
✓ about-us.html
✓ handmade-shoes.html
✓ tuxedos.html
✓ wedding-dresses.html
✓ gallery.html
✓ appointment-form.html
Done.
```

If any AssertionError fires, the pattern wasn't found in that file — check the file's nav markup against the OLD_ strings in the script and correct the mismatch.

- [ ] **Step 3: Verify nav-left replacement**

```bash
grep -c "nav-trigger" index.html about-us.html gallery.html
```

Expected: `2` for each page (one Men trigger, one Women trigger).

- [ ] **Step 4: Verify mega panels inserted**

```bash
grep -c "mega-panel" index.html about-us.html gallery.html
```

Expected: `4` for each page (two `<div class="mega-panel">` open tags + two `id=` attributes — or grep `id=\"mega-` for a cleaner check):

```bash
grep -c 'id="mega-' index.html about-us.html gallery.html
```

Expected: `2` for each page.

- [ ] **Step 5: Verify mobile Women expanded**

```bash
grep -c "Bridal Alterations" index.html about-us.html gallery.html
```

Expected: `1` for each page.

- [ ] **Step 6: Verify no old dropdown markup remains**

```bash
grep -rn "has-dropdown" index.html about-us.html gallery.html | grep -v "mobile-menu"
```

Expected: no output (`.has-dropdown` no longer exists outside `.mobile-menu` context).

- [ ] **Step 7: Open in browser and test**

Navigate to `http://localhost:8080/index.html`

Check:
- "Men" and "Women" buttons appear in nav-left in place of the old text links
- Clicking "Men" opens the mega panel (4-column grid with gold labels)
- Clicking "Women" closes Men panel and opens Women panel (2-column Bridal/Fashion)
- Clicking × closes the open panel
- Clicking anywhere outside the nav closes the panel
- Pressing Escape closes the panel
- Clicking "Men" when already open closes it (toggle)
- Chevron rotates down when open, returns when closed
- Nav turns gold on active trigger
- Mobile hamburger → mobile menu still works; Women accordion shows 5 items
- Nav scroll behaviour (transparent → solid) still works

- [ ] **Step 8: Commit**

```bash
git add index.html custom-suits.html ready-to-wear-suits.html about-us.html handmade-shoes.html tuxedos.html wedding-dresses.html gallery.html appointment-form.html scripts/update-nav.py
git commit -m "feat: replace dropdown nav with click-toggle mega panels across all 9 pages"
```

---

## Self-Review

**Spec coverage:**
- [x] Desktop `.has-dropdown:hover` dead zone removed — Task 1
- [x] `.nav-trigger` buttons with `data-panel`, `aria-expanded`, `aria-controls` — Task 3
- [x] Men mega panel: 4 columns (Bespoke / RTW / Footwear / Formal) with correct hrefs — Task 3
- [x] Women mega panel: 2 columns (Bridal + Fashion), 5 links total — Task 3
- [x] New Women items (Bridal Alterations, Accessories, Mother of the Bride, RTW Suits) as `href="#"` — Task 3
- [x] `closeMegaPanels()` on: trigger click-to-close, × button, outside click, Escape — Task 2
- [x] `aria-expanded` on trigger and `aria-hidden` on panel kept in sync — Task 2
- [x] Chevron rotates via `.active` class — Task 1 CSS + Task 2 JS
- [x] Mobile accordion Women items expanded to 5 — Task 3
- [x] All 4 existing nav.js behaviors preserved — Task 2 (existing 38 lines untouched)
- [x] All 9 HTML pages updated — Task 3

**Placeholder scan:** `href="#"` on new Women items is intentional — documented as "no dedicated pages yet."

**Type consistency:** `closeMegaPanels` function defined once in nav.js Task 2, referenced in the three event listeners in the same block. No cross-task naming conflicts.
